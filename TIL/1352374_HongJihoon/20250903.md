## 📅 2025-09-03(수) TIL
- 정적 데이터(CSV) → DB 적재 자동화 PoC 진행
    - 테이블 스키마 설계·생성(예: trade_area_sales_cd): 컬럼/타입/PK·인덱스 정의
    - pandas.read_csv(EUC-KR) → dtype 매핑 → SQLAlchemy로 to_sql(chunksize, method='multi') 배치 INSERT
    - idempotent 적재: UNIQUE KEY + ON DUPLICATE KEY UPDATE, 공백/NULL 정리

    - 기본 검증 쿼리: COUNT(*), DISTINCT STDR_YYQU_CD 등으로 로드 확인

### 🖊️ 결론
- 정적 데이터는 CSV→DB 파이프라인을 파이썬으로 자동화하며 테이블 생성부터 대용량 INSERT, 기초 검증까지 한 사이클을 직접 구축·학습했다.